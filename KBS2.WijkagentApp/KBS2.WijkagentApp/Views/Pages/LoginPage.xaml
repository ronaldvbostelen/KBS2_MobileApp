<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:KBS2.WijkagentApp.ViewModels;assembly=KBS2.WijkagentApp"
             x:Class="KBS2.WijkagentApp.Views.Pages.LoginPage">
    <ContentPage.BindingContext>
        <viewModels:LoginViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Login Portal" FontSize="25" 
                   VerticalOptions="Start" HorizontalTextAlignment="Center"/>
            <StackLayout Margin="20" VerticalOptions="CenterAndExpand">
                <Label Text="Username" />
                <Entry Placeholder="username" Text="{Binding Officer.UserName}"/>
                <Label Text="Password" />
                <Entry IsPassword="true" Text="{Binding Officer.Password}" ReturnCommand="{Binding LoginCommand}" Placeholder="password"/>
                <Button Text="Login" Command="{Binding LoginCommand}" />
                <Label Text="{Binding LoginMessage}" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>