<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:KBS2.WijkagentApp.ViewModels;assembly=KBS2.WijkagentApp"
             x:Class="KBS2.WijkagentApp.Views.Pages.MessagePage"
             NavigationPage.HasNavigationBar="False">
    <ContentPage.BindingContext>
        <viewModels:MessageViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout
            VerticalOptions="End">
            <Button Command="{Binding AddCommand}" Text="ADD ONE"/>
            <Button Command="{Binding DeleteCommand}" Text="DELETE ONE"/>
            <Label Text="Logged in as:"/>
            <Label Text="{Binding UserName, StringFormat='{}{0:N}'}"/>
            <Button Text="LOGOUT" Command="{Binding LogoutCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>