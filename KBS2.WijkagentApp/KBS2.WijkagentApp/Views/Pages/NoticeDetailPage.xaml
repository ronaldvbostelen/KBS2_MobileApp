<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:KBS2.WijkagentApp.ViewModels"
             x:Class="KBS2.WijkagentApp.Views.Pages.NoticeDetailPage"
             Title="Detail melding">
    <ContentPage.BindingContext>
        <viewModels:NoticeDetailViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Text="{Binding Type}"
                   Margin="16,0"
                   Grid.Row="0" 
                   Grid.Column="0" />

            <Label Text="{Binding Description}"
                   Margin="16,0"
                   Grid.Row="1" 
                   Grid.Column="0" />

            <StackLayout Grid.Column="0" Grid.Row="2">
                <TableView Intent="Data">
                    <TableRoot>
                        <TableSection Title="Betrokkenen">
                            <!-- Color needs to be changed with a custom renderer-->
                            <ViewCell>
                                <Grid Margin="16,0">
                                    <StackLayout Orientation="Horizontal">
                                        <Label Text="{Binding Suspect}" />
                                        <Label Text="Verdachte"
                                           HorizontalOptions="EndAndExpand" />
                                    </StackLayout>
                                </Grid>
                            </ViewCell>
                            <ViewCell>
                                <Grid Margin="16,0">
                                    <StackLayout Orientation="Horizontal">
                                        <Label Text="{Binding Victim}" />
                                        <Label Text="Slachtoffer"
                                           HorizontalOptions="EndAndExpand" />
                                    </StackLayout>
                                </Grid>
                            </ViewCell>
                        </TableSection>
                    </TableRoot>
                </TableView>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>